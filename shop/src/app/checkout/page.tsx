import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Checkout" };

export default function CheckoutPage() {
  return (
    <ComingSoon
      title="Checkout"
      kicker="Checkout"
      note="The checkout and payment flow is part of the shop rollout — it arrives in an upcoming prompt. Your cart is safely saved until then."
    />
  );
}
