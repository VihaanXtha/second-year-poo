services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - circuit-bazaar

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - VITE_API_URL=http://localhost:8000
    networks:
      - circuit-bazaar

  vendor:
    build:
      context: ./vendor
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - VITE_API_URL=http://localhost:8000
    networks:
      - circuit-bazaar

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/storage:/app/storage
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - DB_CONNECTION=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=circuit_bazaar
      - DB_USERNAME=circuit
      - DB_PASSWORD=circuit
      - MAIL_FROM_ADDRESS=noreply@circuitbazaar.com
      - MAIL_FROM_NAME="Circuit Bazaar"
    command: sh -c "php artisan serve --host=0.0.0.0 --port=8000"
    depends_on:
      - mysql
    networks:
      - circuit-bazaar

  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=circuit_bazaar
      - MYSQL_USER=circuit
      - MYSQL_PASSWORD=circuit
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - circuit-bazaar

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
      - "8080:80"
    environment:
      - PMA_HOST=mysql
      - MYSQL_ROOT_PASSWORD=root
    depends_on:
      - mysql
    networks:
      - circuit-bazaar

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - admin
      - vendor
      - backend
    networks:
      - circuit-bazaar

volumes:
  mysql_data:

networks:
  circuit-bazaar:
    driver: bridge